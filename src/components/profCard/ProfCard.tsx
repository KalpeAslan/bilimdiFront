import React from 'react'

interface ProfCard {
    imgSrc: any,
    profName: string,
    profArea: string
}

export function ProfCard ({imgSrc, profName, profArea}): React.ReactChildren<ProfCard> {
    return
}